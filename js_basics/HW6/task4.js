// Create a person object with properties
let person = {
    firstName: "Ім'я",
    lastName: "Прізвище",
    age: 30
};

// Add the email property to the person object
person.email = "email@example.com";

// Remove the age property from the person object
delete person.age;

// Log the updated person object to the console
console.log(person);
